package cortana.storage;

//import cortana.exception.CortanaException;
//import cortana.task.TaskList;
//
//import org.junit.jupiter.api.Test;
//
//import java.io.IOException;
//
//import static org.junit.jupiter.api.Assertions.*;

class FileHandlerTest {

//    @Test
//    void testValidateLineFormat_validAndInvalid() {
//        FileHandler fh = new FileHandler(null);  // Adjust if needed
//
//        assertDoesNotThrow(() -> fh.validateLineFormat("T | 1 | Read Book"));
//
//        Exception ex = assertThrows(Exception.class, () -> fh.validateLineFormat("X | 1 | Invalid"));
//        assertTrue(ex.getMessage().contains("Invalid task type"));
//
//        ex = assertThrows(Exception.class, () -> fh.validateLineFormat("T | 2"));
//        assertTrue(ex.getMessage().contains("Malformed line"));
//    }

    // TODO: For loadTasks(), test could be integration style or mocking file reads
}
